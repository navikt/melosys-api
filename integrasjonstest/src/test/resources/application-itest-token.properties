mockserver.port=8182

# AAREG
arbeidsforhold.rest.url=http://localhost:${mockserver.port}

# PDL
PDL.url=http://localhost:${mockserver.port}/graphql

#REST-STS
REST_STS.url=/bare-brukt-for-hente-variable

# SAF
SAF.url=http://localhost:${mockserver.port}

# GSAK web service
SakAPI_v1.url=http://localhost:${mockserver.port}/api/v1/saker
OppgaveAPI_v1.url=http://localhost:${mockserver.port}/api/v1

# MEDL2
medlemskap.rest.url=http://localhost:${mockserver.port}/medl2/api/v1/medlemskapsunntak
